package mffs.particle

import nova.core.render.Color

/**
 * @author Calclavia
 */
object FieldColor {
	val red = Color.rgb(255, 0, 0)
	val green = Color.rgb(0, 255, 0)
	val blue = Color.rgb(127, 240, 255)
}
