package mffs.render

import nova.core.render.Color

/**
 * @author Calclavia
 */
object FieldColor
{
	val red = new Color(1f, 0f, 0f)
	val green = new Color(0f, 1f, 0f)
	val blue = new Color(0.5f, 0.9f, 1f)
}
