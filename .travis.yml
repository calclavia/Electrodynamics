language: java
jdk: oraclejdk8
sudo: false

before_install: chmod +x gradlew
install:
  - ""
script:
  - ./gradlew --continue check javadoc jacocoTestReport

cache:
  directories:
  - ~/.gradle

notifications:
  email: false
